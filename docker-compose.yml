version: '3.8'

services:
  streamlit-buyer-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamlit-buyer-agent
    ports:
      - "8503:8501"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
